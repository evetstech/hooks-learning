import {
  useEffect,
  useCallback,
  useState
} from 'react';
import { fetchEndpoint } from '../common/functions';

/**
 * A hook to allow for fetching an endpoint (uses fetchEndpoint function)
 * allows for manual trigger of fetch as well via callback prop
 */
const useFetchApi = () => {
  //data state

  //trigger the fetch function

  //return stuff
}

export default useFetchApi;