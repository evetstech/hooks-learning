import { useState, useCallback } from 'react';

/**
 * A hook to allow for true/false state management, triggers toggle via function
 */
const useToggler = () => {
  //some type of toggle state

  //some type of toggle callback to allow for triggering

  //return state and trigger function
};

export default useToggler;